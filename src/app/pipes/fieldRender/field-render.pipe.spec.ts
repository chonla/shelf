import { FieldRenderPipe } from './field-render.pipe';

describe('FieldRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
